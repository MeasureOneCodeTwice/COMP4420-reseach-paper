\documentclass[11pt, letterpaper]{article}

% --- Packages ---
\usepackage[margin=1in]{geometry} % Standard 1-inch margins
\usepackage{amsmath, amssymb}     % Math formatting
\usepackage{graphicx}             % Figures
\usepackage{listings}             % Code blocks
\usepackage{wrapfig}              % Wrapping
\usepackage[dvipsnames]{xcolor}               % Code highlighting colors
\usepackage{hyperref}
\usepackage{caption}
\usepackage{tikz}
\usetikzlibrary{matrix}
\usetikzlibrary{positioning}
% \usepackage{parskip}
\usepackage[style=ieee, backend=biber]{biblatex}
\addbibresource{bibliography.bib}
\graphicspath{{../figures/}}


% --- Code Block Styling ---
\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}

\lstdefinestyle{mystyle}{
    backgroundcolor=\color{backcolour},   
    commentstyle=\color{codegreen},
    keywordstyle=\color{magenta},
    numberstyle=\tiny\color{codegray},
    stringstyle=\color{codepurple},
    basicstyle=\ttfamily\footnotesize,
    breakatwhitespace=false,         
    breaklines=true,                 
    captionpos=b,                    
    keepspaces=true,                 
    numbers=left,                    
    numbersep=5pt,                  
    showspaces=false,                
    showstringspaces=false,
    showtabs=false,                  
    tabsize=2
}
\lstset{style=mystyle}
\allowdisplaybreaks

% --- Document Info ---
\title{Cache Optimization: Are Our Algorithms Realistic?\\
\large \textcolor{red}{(need some tagline)}}
\author{Juan Rempel \and Logan Decock}
\date{\today}

\begin{document}

\maketitle

\begin{abstract}

    \textcolor{red}{(need to fill in the abstract)}

\end{abstract}

\section{Introduction}

% Hardware advancements have scaled unevenly over the decades, with processor clock speeds advancing much faster than memory fetching speeds~\cite{wulf_memorywall}. This problem is exacerbated when data sizes grow into terabyte, petabyte, or lately, exabyte levels. Distributed architectures are increasingly common, where storage is separated from computation. While the original problem can be motivated by the hardware and data on a home computer, it becomes exponentially costlier when external networks become the data bus with network latency.

Hardware advancements have scaled unevenly over the decades, with processor clock speeds advancing much faster than memory fetching speeds~\cite{wulf_memorywall}. This problem is exacerbated when data sizes grow into terabyte, petabyte, or lately, exabyte levels~\cite{boureima_exabyte}. Distributed architectures are increasingly common, where storage is separated from computation, including the complete disaggregation of memory~\cite{shan_disaggregation}. While the original problem can be motivated by the hardware and data on a home computer, it becomes exponentially costlier when external networks become the data bus with network latency.

Even at a small scale, consider a modern home computer with components purchased within the last year, equipped with Viper Xtreme 5 DDR5 6000MT/s CL30 memory and an AMD Ryzen 5 9600X 5.4GHz processor. According to standard latency calculations, fetching a block of memory for the first time requires 10 ns: 
\[
    \text{Latency (ns)} = \frac{2000 \cdot \text{CAS Latency}}{\text{RAM speed in MT/s}} = \frac{2000 \cdot 30}{6000} = 10 \text{ ns} 
\]
Within that same 10 ns time frame, the processor has already completed 54 clock cycles, as derived from its clock speed of 5.4 GHz. Without advanced hardware prefetchers, this would mean that the processor stalls for at least 54 clock cycles while waiting for data. And this is worse when you consider that all you likely wanted was a single word in the fetched block, underutilizing all the words that came alongside in that same block.

This reveals a problem with the Word RAM model of algorithm analysis. It considers all memory fetches to be completely free, assigning costs only to processor instructions. Clearly this is a na\"{\i}ve view of the real world time complexity of this algorithm, as an algorithm that thrashes the memory would underperform one of similar Word RAM model runtime complexity that consumes all words in every block it fetches.

In this paper, we will investigate cache optimization through the lens of the Cache-Oblivious model. Then we will explore a few algorithms, namely \textit{lazy funnelsort}, and examine what makes them cache optimal on all hardware. We will focus the algorithms section primarily on the lazy funnelsort algorithm, including our attempt at implementing it. While our original proposal targeted benchmarking this algorithm alongside other cache-oblivious algorithms, the complexity of designing and implementing optimal cache-oblivious algorithms proved challenging and time-consuming. Therefore, we will discuss the challenges we faced during the design and implementation of lazy funnelsort, the gap between the theoretical funnelsort model and real-world programming, and potential for future work built upon our groundwork.

\section{The Cache-Oblivious Model}
% 1.5 - 2 pages
% - 0.5pg External Memory Model
% - 1pg Cache-Oblivious Model

Since cache optimality is our goal, we have already seen why the Word RAM model of analysis is not the optimal lens by which to analyze this. Though we have already alluded to us wanting to be oblivious to hardware specifications with our analysis, forcing the algorithms to be cache optimal regardless of the hardware it is running on, we believe that the cache-oblivious model is best understood intuitively by building up from analysis models that \textit{do} know hardware specifications. Now that we have our basic memory layout defined, we can start with the External Memory Model.

The External Memory Model observes only two arbitrary levels within the cache hierarchy~\cite{demaine}. The higher level of these two levels will be our cache, and the lower level will be our memory~\ref{fig:cache-visual}. Our cache has a total size of $M$ words, and we assume the memory contains $> M$ words~\cite{demaine}. Data is stored and transferred in chunks, called blocks. Each block consists of $B$ words~\cite{demaine}. Thus, our cache has $M/B$ cache lines, which is where blocks are stored when fetched from memory~\cite{demaine}. 

% External Memory Model: program must decide which cache line to evict
Every time a program needs data for the first time, it will need to load that from memory. Since it cannot simply fetch that word by itself, the entire block of $B$ words, one of which is the word the program requested, is transferred. If all cache lines are occupied, then the program must also first decide which cache line to empty by evicting the block from this cache line~\cite{demaine}. Since the program is only interested in one word, it wastes the expensive memory transfer of all $B-1$ words that were unutilized in this block.

The cost of transferring this block across the bus, and the associated latency of requesting it from memory, would be better amortized if we were able to use all $B$ words within this block. And we would like to do that without even knowing the hardware specific $B$ and $M$ parameters.

\begin{figure}[htbp]
        
    \centering

    \begin{tikzpicture}
        % cache
        \matrix (cache) [matrix of nodes, 
                    nodes={inner sep=0pt, minimum width=0.1cm, minimum height=0.4cm, anchor=center}, 
                    nodes in empty cells,
                    column sep=0pt, 
                    row sep=0pt,
                    row 6/.style={nodes={fill=BurntOrange!40}}] 
        {
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&& \\ % line 1
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 2
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 3
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 4
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 5
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 6
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 7
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 8
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 9
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 10
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 11
            %  &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 12
            %  &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 13
            %  &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 14
            %  &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 15
            %  &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 16
        };

        % rectangle lines
        \draw (cache-1-1.north west) -- (cache-1-31.north east);
        \draw (cache-1-31.north east) -- (cache-11-31.south east);
        \draw (cache-11-31.south east) -- (cache-11-1.south west);
        \draw (cache-11-1.south west) -- (cache-1-1.north west);

        % row lines
        \foreach \i in {1, ..., 11} {
            \draw (cache-\i-1.south west) -- (cache-\i-31.south east);
        }

        % label
        \node (cachelabel) at ([yshift=0.1cm]cache.north) {\textbf{Cache}};

        % width
        \draw[<->, thick] ([yshift=-0.3cm]cache-11-1.south west) -- ([yshift=-0.3cm]cache-11-31.south east) node[midway, below] {$1$ block $ = B$ words};

        % height
        \draw[<->, thick] ([xshift=-0.3cm]cache-1-1.north west) -- ([xshift=-0.3cm]cache-11-1.south west) node[midway, left] {$\frac{M}{B}$ cache lines};

        % memory
        \matrix (memory) [right=3cm of cache,
                    matrix of nodes, 
                    nodes={inner sep=0pt, minimum width=0.1cm, minimum height=0.4cm, anchor=center}, 
                    nodes in empty cells,
                    column sep=0pt, 
                    row sep=0pt,
                    row 3/.style={nodes={fill=Brown!40}}] 
        {
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&& \\ % line 1
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 2
            &&&&&&&&&&&&&&&&&&&&&&&&&|[fill=ForestGreen!30]|&&&&&  \\ % line 3
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 4
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 5
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 6
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 7
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 8
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 9
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 10
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 11
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 12
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 13
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 14
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 15
            &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&  \\ % line 16
        };

        % rectangle lines
        \draw (memory-1-1.north west) -- (memory-1-31.north east);
        \draw (memory-1-31.north east) -- (memory-16-31.south east);
        \draw (memory-16-31.south east) -- (memory-16-1.south west);
        \draw (memory-16-1.south west) -- (memory-1-1.north west);

        % row lines
        \foreach \i in {1, ..., 15} {
            \draw (memory-\i-1.south west) -- (memory-\i-31.south east);
        }

        % label
        \node (memorylabel) at ([yshift=0.1cm]memory.north) {\textbf{Memory}};

        % message box
        \node (message) at ([xshift=1.8cm, yshift=0.1cm]memory.north east) {\textcolor{ForestGreen}{only want this 1 word}};

        % arrows
        % message -> word we want
        \draw[<-, thick, ForestGreen] (memory-3-26.north) to[out=90, in=180] (message.west);

        % retrieve cache
        \draw[<-, thick, Brown!40] ([xshift=0.1cm, yshift=0.1cm]cache-6-31.east) to[out=30, in=180] node[midway, fill=white, text=gray]{\tiny{fetch block}} ([xshift=-0.1cm]memory-3-1.west);

        % evict cache
        \draw[->, thick, BurntOrange!40] ([xshift=0.1cm, yshift=-0.1cm]cache-6-31.east) to[out=-30, in=180] node[midway, fill=white, text=gray]{\tiny{evict cached block}} ([xshift=-0.1cm]memory-14-1.west);

    \end{tikzpicture}

    \vspace{-2pt}
    \footnotesize{\textcolor{gray}{Drawing aided by Gemini}}
    \captionof{figure}{Representation of the cache}
    \label{fig:cache-visual}
\end{figure}

% Ideal cache: machine where programs can run either cache-aware, or cache-oblivious
So far under the External Memory Model, the program is responsible for the optimal cache replacement strategy~\cite{demaine}. We can abstract this responsibility away and remove knowledge of hardware parameters by introducing the Cache-Oblivious Model~\cite{demaine}. 

Similar to the External Memory Model, the Cache-Oblivious Model is a theoretical machine that consists only of two levels of cache hierarchy, the small, fast cache and the slow, large memory~\cite{demaine}.\footnote{Cache obliviousness was originally introduced as a chosen property of the Ideal-Cache Model by Frigo et al., where the model was still aware of hardware parameters but the algorithms could choose to ignore them, thus making them cache oblivious~\cite{frigo}. We will follow Demaine's lead in introducing the Cache-Oblivious Model to hold the cache oblivious properties built atop the Ideal-Cache Model~\cite{demaine}.} 

Contrary to the External Memory Model, the Cache-Oblivious Model places the responsibility of the optimal cache replacement strategy on the machine itself and not the programs run on it~\cite{demaine}. This optimal cache replacement strategy is one where the cache line holding the block that will be used furthest in the future is the one that is evicted next, ensuring temporal locality~\cite{demaine}. Demaine assures us that this strategy can be substituted, without loss of optimality, with the Least Recently Used (LRU) eviction strategy~\cite{demaine}. 

The Cache-Oblivious Model also takes on the Tall Cache assumption, $M \in \Omega(B^2)$, which assumes that the cache itself contains $\ge B^2$ words~\cite{demaine}. This will ensure that the number of cache lines $= M/B \ge B^2/B = B$, meaning that it is at least as tall as it is wide. 

Finally, the Cache-Oblivious Model also assumes that the cache is fully associative, which is $M/B$-way associative, which ensures that any block in memory can be allocated to any cache line~\cite{demaine}. 

Frigo et al. and Demaine observe that a beneficial side-effect of algorithms being cache oblivious is that they are optimal across all levels of cache, not just the two levels within our theoretical model~\cite{frigo,demaine}. The reason this property unfolds is that we can examine \textit{any} two levels of the cache hierarchy with our Cache-Oblivious Model. Since the algorithm within the Cache-Oblivious Model is cache optimal without any knowledge of hardware parameters, namely the block size $B$ or cache size $M$, then the algorithm must be cache optimal across any two adjacent levels of the cache hierarchy. This side effect holds as long as the cache hierarchy maintains the \textit{inclusion property}, meaning that any block stored in a higher, faster cache level is also stored in the lower, slower memory level~\cite{frigo,demaine}.

With the Cache-Oblivious Model in hand, we can now build programs on top of it without ever relying on hardware parameters, $B$ and $M$, to make them cache optimal. Thus, we have the foundation by which we will study \textit{lazy funnelsort}.

Before examining \textit{lazy funnelsort}, we must first understand the underlying memory layout that enables its cache optimality: the cache-oblivious van Emde Boas trees.

\section{Cache-Oblivious van Emde Boas Trees}
% 1 - 1.5pg

To lay the groundwork for the data structure that enables a cache optimal \textit{lazy funnelsort} algorithm, we need to examine cache-oblivious van Emde Boas (vEB) trees. We assume the reader is already familiar with standard vEB trees, as are usually studied within the Word RAM model. We will adapt the vEB data structure to the Cache-Oblivious Model to optimally store large datasets. Accordingly, we will focus only on the static vEB tree, exploring only its memory layout and space complexity while ignoring dynamic operations.

A cache-oblivious vEB tree of $N$ nodes is recursively divided into a \textit{summary} vEB tree of size $\sqrt{N}$ and $\sqrt{N}$ \textit{cluster} vEB trees, each also of size $\sqrt{N}$~\cite{demaine}. Conceptually, the tree's height is divided in half, with the summary tree forming the top half and the clusters forming the bottom half~\cite{demaine} (Figure~\ref{fig:veb-tree}). To optimally store this data, these subtrees are laid out sequentially within a single contiguous segment of memory: the summary tree is placed first, followed immediately by the cluster trees ordered from left to right~\cite{demaine} (Figure~\ref{fig:veb-layout}).

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{image6.png}
    \captionof{figure}{vEB layout (left) and vEB tree of height 5 (right). Reproduced from Demaine~\cite{demaine}.}
    \label{fig:veb-tree}
\end{figure}

\begin{figure}[htbp]
        
    \centering

    \begin{tikzpicture}
        % layout
        \matrix (memory) [matrix of nodes, 
                    nodes={draw, minimum width=0.1cm, minimum height=1cm, anchor=center}, 
                    nodes in empty cells,
                    column sep=0pt, 
                    row sep=0pt] 
        {
            Summary & Cluster $1$ & Cluster $2$ & \ldots & Cluster $\sqrt{N}$ \\
        };

    \end{tikzpicture}

    \vspace{-2pt}
    \captionof{figure}{Memory layout of an $N$-node vEB tree}
    \label{fig:veb-layout}
\end{figure}

Since the vEB tree recurses into smaller subtrees stored sequentially in memory, the recursion guarantees that at some level, the subtrees will reach a size of $\le B$ words~\cite{demaine}. This subtree would therefore occupy at most two consecutive memory blocks as it may not be perfectly block aligned, and thus require at most two memory transfers to load the entire subtree into cache~\cite{demaine}. Any subsequent memory operations within this subtree would require no additional memory transfers, having an $O(1)$ cache complexity. 

We are now well equipped to study the \textit{lazy funnelsort} algorithm. The vEB memory layout will be key in making the \textit{lazy funnelsort} cache optimal.

\section{Lazy Funnelsort}
% 2.5 - 3 pages
% - 0.5pg Intro

Funnelsort may be a novel sorting algorithm, but its roots are in mergesort. Let's build up to the funnelsort algorithm through targetting cache optimality in mergesort.

Firstly, lets analyse in depth the most basic form of merge sort, a two way merge sort. For an input array of size $n$ this algorithm recursively splits the input array into $2$ halves of size $h$. These halves are sorted (by merge sort) and merged together by performing a linear scan on each input array and writing in a linear scan to an output array. Given the array is not already in cache, scanning each input array costs $\frac{h}{B}$ memory transfers and writing to the output array costs $\frac{2h}{B}$ memory transfers. From this we create the cost tree in Figure \ref{fig:two-way-merge-sort-cost-tree}. The cost of each layer of the tree sums to $4\frac{n}{B}$ and the tree has height $log_2n$.
\begin{align*}
    && B &\geq 
    \frac{n}{2^d} + 2^1\frac{n}{2^{d + 1}} + 2^2\frac{n}{2^{d + 2}} + \cdots + 2^{(  \log_2n - d ) }\frac{n}{2^{d + (\log_2n - d)}} 
    \\ 
    & \implies 
    & B &\geq 
    (\log_2n - d)\frac{n}{2^{d}}
    \\ 
    & \implies 
    & B &\geq 
    \frac{n}{2^{d}}
    \\ 
    & \implies 
    & \frac{B}{n} &\geq 
    \frac{1}{2^{d}}
    \\ 
    & \implies 
    & 2^{d} &\geq
    \frac{n}{B} 
    \\ 
    & \implies 
    & d &\geq
    \log_2\frac{n}{B} 
\end{align*}
\begin{wrapfigure}{r}{0.5\textwidth}
\begin{center}
\begin{scalebox}{0.7}{
    \begin{tikzpicture}[
      level 1/.style={sibling distance=5cm},
      level 2/.style={sibling distance=2.5cm},
      level 3/.style={sibling distance=1.25cm},
      every node/.style={draw, circle},
    ]
    \node { $4\frac{n}{B}$ }
        child { node { $2\frac{n}{B}$  }
          child { node { $\frac{n}{B}$  }
             child { node { $\frac{n}{2B}$ } }
             child { node { $\frac{n}{2B}$ } }
          }
          child { node { $\frac{n}{B}$  }
              child { node { $\frac{n}{2B}$ } }
              child { node { $\frac{n}{2B}$ } }
            }
          }
        child { node { $2\frac{n}{B}$  }
          child { node { $\frac{n}{B}$  }
             child { node { $\frac{n}{2B}$ } }
             child { node { $\frac{n}{2B}$ } }
          }
          child { node { $\frac{n}{B}$  }
              child { node { $\frac{n}{2B}$ } }
              child { node { $\frac{n}{2B}$ } }
            }
          };
    \end{tikzpicture}
}
\captionof{figure}{A 2-way merge sort cost tree}
\label{fig:two-way-merge-sort-cost-tree}
\end{scalebox}
\end{center}
\end{wrapfigure}
Before calculating the total cost we must also consider the case where an instance is small enough that it and its subproblems fit in a single block of cache. In this case, the subproblems are free. This occurs at some depth $d$ such that

So in total there are $\log_2n - \log_2(\frac{n}{B}) - 1 = \log_2(B) - 1$ costless levels in the tree.
Subtracting that from the total number of levels in the tree we obtain $\log_2(n) - \log_2(B) - 1 = \log_2\frac{n}{B} - 1$ non-costless levels,
each of which costing $4\frac{n}{B}$ memory transfers for a total cost of $4\frac{n}{B}\log_2\frac{n}{B} \in \Theta(\frac{n}{B}\log_2\frac{n}{B})$ memory transfers.


While optimal in terms of work cost, this algorithm is not optimal in terms of memory transfers. 
We can improve this algorithm by using a $\frac{M}{B} - 1$ way merge sort. While merging, a $\frac{M}{B} - 1$ keeps one block from each subproblem and one block of the output array in cache at once, completely filling it up and keeping the local cost to $\frac{n}{B}$. Since the recursion tree has branch factor of $\frac{M}{B}$ its height is $\log_\frac{M}{B}\frac{n}{B}$ and the total cost for sorting is $\frac{n}{B}\log_\frac{M}{B}\frac{n}{B}$. 
This results matches the cache oblivious sorting lower bound ~\cite{frigo}. 
Unfortunately, as in the name of the cache oblivious model, our algorithms must be oblivious to $M$ and $B$. Since this algorithm requires knowledge of both variables it isn't cache oblivious. Fortunately, we can use the idea of a $\frac{M}{B}$ to create a cache oblivious with the same, optimal asymptotic cost.


\subsection{How it works}
Similar to merge sort, funnel sort recursively splits the input into equal sized chunks and merges them together.
The K-Funnel data structure is how funnel sort simulates a $\frac{M}{B}$ way merger and is the bulk of the of the complexity of the algorithm.
Each K-Funnel takes $K$ sorted input buffers of size $K^2$ and merges them into to a single buffer of size $K^3$.
The K-Funnel's recursive structure is similar to that of a Emde Boas tree.
\begin{wrapfigure}[17]{r}{0.5\textwidth}{
    \centering
    \includegraphics[width=0.825\linewidth]{image4.png}
    \captionof{figure}{The internal structure of a $n^\frac{1}{3}$ funnel}
    \label{fig:funnel-visual}
}
\end{wrapfigure}
A K-Funnel is comprised of $\sqrt{k} + 1$ $\sqrt{k}$ funnels.
It has 1 top funnel and $\sqrt{k}$ bottom funnels.
Each bottom funnel merges $\sqrt{k}$ of the K-Funnel's input buffers and writes them to a size $(\sqrt{k})^3$ output buffer.
These internal buffers, of which there are $\sqrt{k}$ are used as inputs to the top $\sqrt{k}$ funnel which merges the their contents into the K-Funnel's output buffer.

\subsection{Spatial complexity}

This entire funnelsort structure is very reminiscent of the vEB trees. As such, the recurrence relation, as we'll discover, is very similar. 

Let's start by examining the local cost of each $K$-funnel. The only space occupied in each instance of a $K$-funnel is the $\sqrt{K}$ number of buffers. Each of these buffers are $K^{3/2}$ in size. Thus, each $K$-funnel instance has a local space complexity of $K^{3/2} \cdot \sqrt{K} = K^{2}$ words.

The funnels are recursively defined such that there is one top $\sqrt{K}$-funnel and $\sqrt{K}$ number of bottom $\sqrt{K}$-funnels. Thus, there are a total of $(\sqrt{K} + 1)$ number of $\sqrt{K}$-funnels. This will continue down until we hit the base case of a $2$-funnel, which is actually just a $2$-way merger with no middle buffers at all, thus a small constant space $O(1)$.

Therefore, we have the recurrence relation

\begin{align*}
    S(K) = 
    \begin{cases}
        O(1), & K \le 2 \\
        (\sqrt{K} + 1)S(\sqrt{K}) + K^2 & K > 2
    \end{cases} 
\end{align*}

As Demaine points out, this recurrence has $O(K)$ leaves, each of which use $O(1)$ space, for a total leaf cost of $O(K)$~\cite{demaine}. Since the root funnel cost is $K^2$, this dominates the space complexity~\cite{demaine}. Thus, $S(K) \in \Theta(K^2)$.

\subsection{Cache complexity}
\begin{wrapfigure}{r}{0.5\textwidth}{
    \centering
    \includegraphics[width=0.48\textwidth]{k-funnel-recursive.png}
    \caption{The recursive structure of a K-Funnel}
    \label{fig:k-funnel-deep}
}
\end{wrapfigure}
As it has been done before, we will not formally prove Funnel sort's cache complexity. Instead, we will try to give the reader an intuitive understanding of how Funnelsort emulates a $\frac{M}{B}$ way merge.

As with Cache-Oblivious van Emde Boas trees at some point in the funnel's recursion instances will be small to fit entirely in cache.
This is the key to Funnel sort's cache complexity.

Funnel sort uses a $k = n^\frac{1}{3}$ funnel as pictured in \ref{fig:funnel-visual}.
A K-Funnel's space complexity is $\Theta(k^2)$. 
By the tall cache assumption our cache has $M \in \Omega(B^2)$ words of memory.
This means a $k = B$ funnel fits entirely in cache.

We can think of our $k$-Funnel as a complete tree of B funnels with branching factor B.
We will inspect the properties this tree, in the same manner we analysed merge-sort's recursion tree.
To start, we know that any complete B-ary tree on n nodes has height $\lfloor log_kB \rfloor$. 

The cost any node contributes to the funnel sort algorithm is equal to the number of times it must fill its output buffer.
Not all nodes have the same output buffer size. For example the root level node has an output buffer of size $n$ while a leaf node has an output buffer of size $k^3$. 

\begin{align*}
&
\text{ From leaf to root the sequence of buffer sizes buffer sizes follows the pattern }
\\ &
B^3    \rightarrow 
B^6    \rightarrow 
B^3    \rightarrow 
B^{12} \rightarrow 
B^3    \rightarrow 
B^6    \rightarrow 
B^3 
\\ &
\text{ which can be verified by inspection of Figure \ref{fig:k-funnel-deep}. }
\\ &
\text{ From this sequence we can derive the number of funnel invocations } 
\\ &
\text{ by using elements that must pass through the buffer as the numerator}
\\ &
\frac{k^{1} n^\frac{2}{3}}   {B^3}    \rightarrow 
\frac{k^{2} n^\frac{2}{3}}   {B^6}    \rightarrow 
\frac{k^{3} n^\frac{2}{3}}   {B^3}    \rightarrow
\frac{k^{4} n^\frac{2}{3}}   {B^{12}} \rightarrow 
\frac{k^{5} n^\frac{2}{3}}   {B^3}    \rightarrow 
\frac{k^{6} n^\frac{2}{3}}   {B^6}    \rightarrow
\frac{k^{7} n^\frac{2}{3}}   {B^3}
\\ &
\text{And multiply by the number of funnels to get the number of invocations per level}
\\ &
\frac{k^{7}k^{1} n^\frac{2}{3}}   {B^3}    \rightarrow 
\frac{k^{6}k^{2} n^\frac{2}{3}}   {B^6}    \rightarrow 
\frac{k^{5}k^{3} n^\frac{2}{3}}   {B^3}    \rightarrow
\frac{k^{4}k^{4} n^\frac{2}{3}}   {B^{12}} \rightarrow 
\frac{k^{3}k^{5} n^\frac{2}{3}}   {B^3}    \rightarrow 
\frac{k^{2}k^{6} n^\frac{2}{3}}   {B^6}    \rightarrow
\frac{k^{1}k^{7} n^\frac{2}{3}}   {B^3} =
\\ &
\frac{k^{8} n^\frac{2}{3}}   {B^3}    \rightarrow 
\frac{k^{8} n^\frac{2}{3}}   {B^6}    \rightarrow 
\frac{k^{8} n^\frac{2}{3}}   {B^3}    \rightarrow
\frac{k^{8} n^\frac{2}{3}}   {B^{12}} \rightarrow 
\frac{k^{8} n^\frac{2}{3}}   {B^3}    \rightarrow 
\frac{k^{8} n^\frac{2}{3}}   {B^6}    \rightarrow
\frac{k^{8} n^\frac{2}{3}}   {B^3}
\\ &
\text{ Notice that each item of the sequence has the same numerator. }
\\ &
\text{ The denominators of this sequence are arranged in the order of a full binary tree  }
\\ &
\text{ where each leaf has value $B^3$ and each step up the tree is the square of a node of the previous height. }
\\ &
\text{ Since a b-ary tree with n items has $\lfloor \log_bn \rfloor$ items we know our k-funnel has less than $2\lfloor \log_bn \rfloor$ items. }
\\ &
\text { Hence our sequence tree has less than $log_2log_m(n)$ items.}
% \\ & 
% test
% \sum_{i=1}^{\log_2\log_bn}
% \frac{1}    {B^5    n^\frac{2}{3}} \rightarrow 
% \frac{1}    {B^2    n^\frac{2}{3}} \rightarrow 
% \frac{1}    {B^5    n^\frac{2}{3}} \rightarrow
% \frac{1}    {B^{-4} n^\frac{2}{3}} \rightarrow 
% \frac{1}    {B^1    n^\frac{2}{3}} \rightarrow 
% \frac{1}    {B^2    n^\frac{2}{3}} \rightarrow
% \frac{1}    {B^5    n^\frac{2}{3}}
\end{align*}
Any given node can have an output buffer of size at most $(n^\frac{1}{3})^\frac{2}{3}$. Further, it can be shown that the average number of times a k-funnel is evoked at any level is $O(n/k)$.

We can multiply the cost by the total number of levels in the tree to obtain $O(n^\frac{1}{3}/k) * \lfloor log_kn \rfloor = ...$
\footnote{Unfortunately, we do not have the math for a proof, but must work backwards from the assumption that funnelsort has the optimal bound of $O(\frac{n}{B}\log_\frac{M}{B}\frac{n}{B})$ proved by other papers.} 

% 1pg 

% 0.5 pg

% 0.5 - 1pg

\textcolor{red}{(need to talk about why funnelsort is cache optimal without knowing $M$ and $B$, maybe even a quick comparison to why the $2$-way mergesort does not become cache optimal at some nested level since it doesn't utilize the max number of cache lines)}

\section{Implementing Funnelsort}
Our choice of of implementation language was guided by two factors. The first being enough control over memory to get the K-Funnel laid out contiguous in vEB order. Secondly, we wanted a relatively high level object oriented programming language. With these considerations in mind, we chose C++. Looking back, we didn't actually need too much fine control over memory, only the guarantee that arrays are laid out contiguously in memory, but we were able to take advantage of C++ in other ways.

We implementation had three phases
The first phase was a rough sketch of a psuedocode implementation. The second phase was fleshing out our psuedocode implementation with a proper, functioning C++ program. The third phase was dedicated to modifying the K-Funnel to use a memory arena instead of the default C++ allocator to have the funnel it stored contiguous in memory.

The first phase of the three was the most intellectually stimulating. The main challenge was simply wrapping our heads around a recursive data structure's possible implementation. This stage started with research and the coalescence of multiple papers explaining the structure of the K-Funnel into Figure \ref{fig:funnel-visual}, an aid which has been useful even during the writing of this paper. The actual "coding" step of this stage was a two hour meeting the majority of which was spent struggling and consulting Figure \ref{fig:funnel-visual}. Then, an epiphany: a funnel should own only its output and not input buffers, followed by 10 minutes of writing psuedocode.

The second stage, aside from resulting in a working prototype, mostly served to improve the precision of our understanding of the K-Funnel. This stage begun by drawing multiple K-Funnels to maximum depth. It was this stage in which we had realisations such as the deepest recursive level of the tree using 2-mergers to merge massive $n^\frac{2}{3}$ element buffers and the top the tree using a 2-merger with a size $n$ output buffer. The main challenge we faced in this stage were the issues resulting from non-power of two input sizes, which resulted in non-integer funnel and buffer. This challenge was solved after extensive experimentation and debugging and \verb|std::ceil()|ing. 



The final stage of our implementation was a massive refactor of our second stage's code. The majority of this stage's work was in the creation of a memory arena and the modification of our K-Funnel to use said arena. It wasn't the memory arena its self which was challenge to implement, but was the calculation of memory required by a K-Funnel and all of its recursive instances. Initially, we planned to improve performance by using funnels smaller than $\sqrt{k}$ when an funnel would have less than $k$ input buffers, which happens when when n is not a power of two. We scrapped this idea due to complexity it introduced in our memory requirement calculations. Next was the modification of the K-Funnel to use the memory arena.
\begin{minipage}{1\textwidth}
\begin{lstlisting}[language=C++, caption=Buffer allocation using memory arenas]
void KFunnel::create_middle_buffers(KFunnelArenas arenas)
{
    this->middle_buffers = arenas.buffer_arena->allocate( 
        this->num_middle_buffers() 
    );
    for(int i = 0; i < this->num_middle_buffers(); i++)
    {
        this->middle_buffers[i] = Buffer(
            arenas.buffer_space_arena->allocate( this->middle_buffer_size() )
        );
    }
}
\end{lstlisting}
\end{minipage}
While not complex, this change took a massive amount of time. It required us to migrate the code from arrays of pointers to the \verb|std::span<T>|, which is a window into a contiguous section of an array. These changes were very costly because they required touching a large breadth of the codebase, usually by chasing and fixing compiler errors, causing another to spring up in its place.

The implementation of funnel sort was very time consuming, but not without benefit. We were forced to understand the structure of the K-Funnel down to the most minute details . In the end our implementation is roughly 4 times slower than a merge sort on \verb|INT_MAX / 256| items, taking 16 seconds where merge sort takes 4. While unable to test with larger datasets, especially using over the network databases as "disk" and main memory as "cache", we believe that our implementation will scale preferably to merge sort when data size and cache miss penalties grow.
\section{Challenges}
Aside from challenges in implementation, which we detailed in the above section we have a single challenge haunt us throughout the entire process of creating this paper. Our goal was an intuitive understanding not of how lazy funnelsort works, but of \textit{why} it achieves the optimal $\frac{n}{B}\log_\frac{M}{B}\frac{n}{B}$ cache complexity.

To this end, we have failed. In section 4.3 Cache Complexity we gave a brief sketch of the beginnings of an intuitive understanding, but were not able to show with the proper math backing that our understanding is correct. Specifically, we were not able to prove that the average number of times an instance of the largest K-Funnel that can fit in cache is $O(n^{2}{3})$.

\section{Limitations and Future Work}
Two items of future work exist. The first, mentioned in section 5 Implementing Funnelsort is further testing on larger datasets with greater, millisecond scale cache miss penalties. We believe that these experiments will yield favourable results for the funnelsort algorithm and server as a greater motivation for the paper.

The second future work item is optimization of our current implementation. One such optimisation is to create funnels smaller than $\sqrt{k}$ when the number data-filled of buffers are less than $k$. Additionally, although we store funnel objects in vEB we do not yet store the buffers in vEB. This is due to a constraint is our original design. Our current buffer ownership paradigm is the funnels own their output buffers, but not input buffers. To store buffers in vEB order this ownership would need to be flipped, with funnels owning their input buffers but not owning their output buffers. 
% 1pg

\section{Conclusion}
% 0.5pg

% Example of how to insert C++ code
% \begin{lstlisting}[language=C++, caption=Some caption]
% int* output = new int[size];
% funnel_sort(size, input, output, print_buffers);
% delete[] output;
% \end{lstlisting}


\printbibliography%

\end{document}
